import { RoutePage } from "@/components/route-page";

export default function ShopPage() {
  return <RoutePage config={{ eyebrow: "Your workspace", title: "Keep a view of what you have made.", description: "Review drafts and published listings in one calm inventory view, with the current product always easy to find.", primaryLabel: "My listings", primaryHref: "/money", sideTitle: "A record that grows with you.", sideCopy: "The web workspace reads the same listing space that the artisan app publishes into." }} />;
}
