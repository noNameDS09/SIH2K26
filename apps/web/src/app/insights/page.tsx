import { RoutePage } from "@/components/route-page";

export default function InsightsPage() {
  return <RoutePage config={{ eyebrow: "Your workspace", title: "Notice what the work is telling you.", description: "Return to true advisor lines and anonymised cluster trends without turning the artisan workspace into a chat window.", primaryLabel: "Insights and advisor", primaryHref: "/settings", sideTitle: "One useful sentence.", sideCopy: "The advisor can be quiet. When it speaks, it speaks from this artisan's own records." }} />;
}
