import { RoutePage } from "@/components/route-page";

export default function MoneyPage() {
  return <RoutePage config={{ eyebrow: "Your workspace", title: "Keep money visible and human.", description: "See sales for this artisan, hear an honest empty state when there are none yet, and keep the record separate from any credit language.", primaryLabel: "Sales and Trade Record", primaryHref: "/insights", sideTitle: "No scores. No shame.", sideCopy: "The Trade Record is a set of unlockable signals, never a credit score." }} />;
}
