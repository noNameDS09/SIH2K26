import { RoutePage } from "@/components/route-page";

export default function DistributePage() {
  return <RoutePage config={{ eyebrow: "Create · 07", title: "Let one link travel further.", description: "Publish the signed card, open its QR destination, share the public link, and prepare shaped exports for future channels.", primaryLabel: "Publish and share", primaryHref: "/shop", steps: ["Sign", "QR", "Share"], sideTitle: "No marketplace pressure.", sideCopy: "KalaSetu helps the story travel. It does not add a cart, payment flow, or buyer account." }} />;
}
